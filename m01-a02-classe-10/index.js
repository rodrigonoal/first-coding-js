//Este código calcula a distância entre dois pontos (x1; y1) e (x2; y2)

let x1 = 1, x2 = 1, y1 = 1, y2 = 4, distancia = Math.sqrt(Math.pow(x2 - x1, 2) + Math.pow(y2 - y1, 2), 2);
console.log(`A distância entre os dois pontos é de ${distancia}.`);



