//Este algoritmo calcula a área total de um cilindro com base em sua altura e raio de suas bases

let raio = 1, altura = 3, areat = 2 * Math.PI * raio * (raio + altura);
console.log(`Um cilindro com o raio das bases de valor ${raio} e ${altura} de altura, tem área total de ${areat}.`);